import 'package:flutter/material.dart';

class CreateAppointmentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create Appointment'),
      ),
      body: Center(
        child: Text('This is the Create Appointment Page'),
      ),
    );
  }
}
